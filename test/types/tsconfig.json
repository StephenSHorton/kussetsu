{
  "//": "Consumer-shaped type-check: mimics a typical Vite/Next app tsconfig and resolves",
  "//2": "`kussetsu` from the BUILT dist (run `npm run build:lib` first; `test:types` does).",
  "//3": "Guards against the <view>/<text> SVG-intrinsic collision regressing (#2 / P0-1).",
  "compilerOptions": {
    "jsx": "react-jsx",
    "module": "ESNext",
    "moduleResolution": "Bundler",
    "target": "ES2022",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "strict": true,
    "skipLibCheck": true,
    "noEmit": true,
    "paths": {
      "kussetsu": ["../../dist/index.d.ts"],
      "kussetsu/*": ["../../dist/*"]
    }
  },
  "include": ["consumer.tsx"]
}
